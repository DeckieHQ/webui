import Ember from 'ember';
import ValidatedInput from '../components/validated-input';

export default ValidatedInput.extend({
  //TODO: focus events are not merged on ember bootsrap datepicker, check later if it is
});
