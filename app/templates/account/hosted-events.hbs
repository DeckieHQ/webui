{{#each model as |event|}}
  {{#link-to 'event' event}}
    <div class='well'>
      {{event.title}}
      {{event.begin_at}}
      {{event.capctity}}
      {{event.ambiance}}
      {{event.level}}
      {{event.category}}
    </div>
  {{/link-to}}
{{/each}}
