<label>{{label}}</label>
{{input value=value type=type focus-out="showError" class="form-control" classBinding="showErrors" placeholder=placeholder}}
{{#if showErrors}}
  {{errors}}
  {{#each serverErrors as |serverError|}}
    {{serverError.message}}
  {{/each}}
{{/if}}
