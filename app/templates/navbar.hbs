<header>
  <nav class='navbar navbar-default'>
    <div class='container-fluid'>
      <div class='navbar-header'>
        {{#link-to 'index' class='navbar-brand'}}Deckie{{/link-to}}
      </div>
      <div>
        <ul class='nav navbar-nav navbar-right'>
          {{#if session.isAuthenticated}}
            <li>{{notifications-box class='notifications'}}</li>
            <li>{{#link-to 'events.new'}}Create event{{/link-to}}</li>
            <li>{{#link-to 'account.dashboard'}}Account{{/link-to}}</li>
            <li><a href="#" {{action 'logout'}}>Logout</a></li>
          {{else}}
            <li>{{#link-to 'registration'}}Registration{{/link-to}}</li>
            <li>{{#link-to 'login'}}Login{{/link-to}}</li>
          {{/if}}
        </ul>
      </div>
    </div>
  </nav>
</header>
