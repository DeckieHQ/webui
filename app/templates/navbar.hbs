<header>
  <nav class='navbar navbar-default'>
    <div class='container-fluid'>
      <div class='container navbar-header'>
        {{#link-to 'index' class='navbar-brand'}}Deckie{{/link-to}}
        <ul class='nav navbar-nav navbar-right'>
          {{#if session.isAuthenticated}}
          <!-- <li>{{#link-to 'clusters'}}Clusters{{/link-to}}</li>
          <li>{{#link-to 'nodes'}}Nodes{{/link-to}}</li>
          <li>{{#link-to 'containers'}}Containers{{/link-to}}</li>
          <li class="dropdown">
            <a href="#" data-toggle="dropdown" class="dropdown-toggle">{{userEmail}} <b class="caret"></b></a>
            <ul class="dropdown-menu">
              <li>{{#link-to 'account.profile'}}Account settings{{/link-to}}</li>
              <li class='divider'></li>
              <li><a {{action 'logout'}}>Logout</a></li>
            </ul>
          </li> -->
            <a href="#" {{action 'logout'}}>Logout</a>
          {{else}}
            {{#link-to 'registration'}}Registration{{/link-to}}
            {{#link-to 'login'}}Login{{/link-to}}
          {{/if}}
        </ul>
      </div>
    </div>
  </nav>
</header>
